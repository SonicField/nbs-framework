#!/bin/bash
# nbs-remote-edit: Edit files on remote machines via scp
#
# Pull a remote file locally, edit it with Claude's normal Edit tool,
# then push it back. Uses pty-session for scp to bypass BpfJailer.
#
# Usage:
#   nbs-remote-edit pull <host> <remote-path>   Download file for local editing
#   nbs-remote-edit push <host> <remote-path>   Upload edited file back
#   nbs-remote-edit diff <host> <remote-path>   Show diff between local and remote
#   nbs-remote-edit help                         Show this help
#
# Environment:
#   NBS_REMOTE_EDIT_DIR   Local staging directory (default: .nbs/remote-edit)
#
# Examples:
#   nbs-remote-edit pull build-host /data/users/dev/project/src/main.cpp
#   # Edit .nbs/remote-edit/build-host
#   nbs-remote-edit push build-host /data/users/dev/project/src/main.cpp
#
# Exit codes:
#   0 - Success
#   1 - General error
#   2 - File not found (remote or local)
#   3 - Transfer failed
#   4 - Invalid arguments

set -euo pipefail

PTY_SESSION="${HOME}/.nbs/bin/pty-session"
STAGING_DIR="$(cd "${NBS_REMOTE_EDIT_DIR:-.nbs/remote-edit}" 2>/dev/null && pwd || echo "$(pwd)/.nbs/remote-edit")"

# Suppress pty-session tool banner
export NBS_PTY_QUIET=1

[[ -x "$PTY_SESSION" ]] || { echo "Error: pty-session not found at $PTY_SESSION" >&2; exit 1; }

usage() {
    grep '^#' "$0" | grep -v '!/bin/bash' | cut -c3-
}

local_path() {
    local host="$1"
    local remote_path="$2"
    [[ "$host" =~ [/] ]] && { echo "Error: host must not contain '/'" >&2; return 4; }
    echo "${STAGING_DIR}/${host}${remote_path}"
}

# Run scp via a short-lived pty-session (gets user's credentials)
run_scp() {
    local session_name="nbs-edit-$$"
    "$PTY_SESSION" create "$session_name" 'bash --login' >/dev/null 2>&1
    sleep 1

    "$PTY_SESSION" send "$session_name" "$1; echo EDIT_RC=\$?" >/dev/null 2>&1

    # Wait for completion
    local elapsed=0
    local timeout=30
    while (( elapsed < timeout )); do
        sleep 2
        elapsed=$((elapsed + 2))
        local output
        output=$("$PTY_SESSION" read "$session_name" 2>/dev/null) || true
        if echo "$output" | grep -q "EDIT_RC="; then
            local rc
            rc=$(echo "$output" | grep -oP 'EDIT_RC=\K[0-9]+' | tail -1)
            "$PTY_SESSION" kill "$session_name" >/dev/null 2>&1 || true
            return "${rc:-1}"
        fi
    done

    "$PTY_SESSION" kill "$session_name" >/dev/null 2>&1 || true
    echo "Error: scp timed out after ${timeout}s" >&2
    return 3
}

cmd_pull() {
    local host="${1:-}"
    local remote_path="${2:-}"

    if [[ -z "$host" ]] || [[ -z "$remote_path" ]]; then
        echo "Error: pull requires <host> and <remote-path>" >&2
        exit 4
    fi

    local lpath
    lpath=$(local_path "$host" "$remote_path")
    mkdir -p "$(dirname "$lpath")"

    if run_scp "scp $(printf '%q' "${host}:${remote_path}") $(printf '%q' "$lpath")"; then
        if [[ -f "$lpath" ]]; then
            echo "$lpath"
        else
            echo "Error: scp succeeded but file not found at $lpath" >&2
            return 1
        fi
    else
        rm -f "$lpath"
        echo "Error: Failed to download ${host}:${remote_path}" >&2
        return 3
    fi
}

cmd_push() {
    local host="${1:-}"
    local remote_path="${2:-}"

    if [[ -z "$host" ]] || [[ -z "$remote_path" ]]; then
        echo "Error: push requires <host> and <remote-path>" >&2
        exit 4
    fi

    local lpath
    lpath=$(local_path "$host" "$remote_path")

    if [[ ! -f "$lpath" ]]; then
        echo "Error: Local file not found: $lpath" >&2
        echo "Did you run 'nbs-remote-edit pull' first?" >&2
        return 2
    fi

    if run_scp "scp $(printf '%q' "$lpath") $(printf '%q' "${host}:${remote_path}")"; then
        echo "Pushed: ${remote_path} -> ${host}"
    else
        echo "Error: Failed to upload to ${host}:${remote_path}" >&2
        return 3
    fi
}

cmd_diff() {
    local host="${1:-}"
    local remote_path="${2:-}"

    if [[ -z "$host" ]] || [[ -z "$remote_path" ]]; then
        echo "Error: diff requires <host> and <remote-path>" >&2
        return 4
    fi

    local lpath
    lpath=$(local_path "$host" "$remote_path")

    if [[ ! -f "$lpath" ]]; then
        echo "Error: Local file not found: $lpath" >&2
        return 2
    fi

    local tmp
    tmp=$(mktemp)
    trap "rm -f $(printf '%q' "$tmp")" RETURN

    if run_scp "scp $(printf '%q' "${host}:${remote_path}") $(printf '%q' "$tmp")"; then
        diff -u "$tmp" "$lpath" --label "remote:${remote_path}" --label "local:${lpath}" || true
    else
        echo "Error: Failed to download ${host}:${remote_path} for diff" >&2
        return 3
    fi
}

# Main dispatch
case "${1:-}" in
    pull)  shift; cmd_pull "$@" ;;
    push)  shift; cmd_push "$@" ;;
    diff)  shift; cmd_diff "$@" ;;
    help|--help|-h|"") usage ;;
    *)
        echo "Unknown command: $1" >&2
        echo "Run 'nbs-remote-edit help' for usage" >&2
        exit 4
        ;;
esac
